<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.parts.zn">

    <application
        android:name="com.parts.zn.one.DaggerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Dagger2 简单使用 Demo -->
        <activity android:name="com.parts.zn.one.MainActivity">
        </activity>

        <!-- RecyclerView 嵌套 EditText 冲突解决 -->
        <activity android:name="com.parts.zn.two.TwoActivity">
        </activity>

        <!-- RecyclerView 嵌套 TagFlowLayout Demo -->
        <activity android:name="com.parts.zn.three.ThreeActivity">
        </activity>

        <!-- LeetCode 算法 -->
        <activity android:name="com.parts.zn.four.FourActivity">
        </activity>

        <!-- AutoCompleteTextView模拟网络模糊查询匹配 -->
        <activity android:name="com.parts.zn.five.FiveActivity">
        </activity>

        <!-- ViewStub 简单使用 -->
        <activity android:name="com.parts.zn.six.SixActivity">
        </activity>

        <!-- 自定义小型组件测试 -->
        <activity android:name="com.parts.zn.seven.SevenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- SVG 矢量图动画使用 -->
        <activity android:name="com.parts.zn.eight.EightActivity">
        </activity>

        <!-- AIDL 简单图书馆 Demo -->
        <activity android:name="com.parts.zn.nine.aidltest.NineActivity">
        </activity>

    </application>

</manifest>